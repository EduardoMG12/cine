# 🎬 CineVerse - Environment Configuration
# Configuration for docker-compose and API v2

# ===========================================
# DOCKER COMPOSE SETTINGS
# ===========================================
# Host ports for services (change if ports are in use)
API_HOST_PORT=8080
POSTGRES_HOST_PORT=5432
REDIS_HOST_PORT=6379
FLUTTER_HOST_PORT=3000

# PostgreSQL Docker Container Settings
POSTGRES_USER=cineverse
POSTGRES_PASSWORD=cineverse123
POSTGRES_DB=cineverse

# ===========================================
# API V2 SERVER CONFIGURATION  
# ===========================================
PORT=8080
HOST=0.0.0.0
ENVIRONMENT=development
READ_TIMEOUT=15s
WRITE_TIMEOUT=15s
SHUTDOWN_TIMEOUT=30s

# 🎬 CineVerse - Environment Configuration
# Configuration for docker-compose and API v2
# Copy this file to .env and update with your actual values

# ===========================================
# DOCKER COMPOSE SETTINGS
# ===========================================
# Host ports for services (change if ports are in use)
API_HOST_PORT=8080
POSTGRES_HOST_PORT=5432
REDIS_HOST_PORT=6379
FLUTTER_HOST_PORT=3000

# PostgreSQL Docker Container Settings
POSTGRES_USER=cineverse
POSTGRES_PASSWORD=cineverse123
POSTGRES_DB=cineverse

# ===========================================
# API V2 SERVER CONFIGURATION  
# ===========================================
PORT=8080
HOST=0.0.0.0
ENVIRONMENT=development
READ_TIMEOUT=15s
WRITE_TIMEOUT=15s
SHUTDOWN_TIMEOUT=30s

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cineverse
DB_USER=cineverse
DB_PASSWORD=cineverse123
DB_SSL_MODE=disable
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME=5

# ===========================================
# REDIS CONFIGURATION
# ===========================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ===========================================
# JWT CONFIGURATION
# ===========================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-must-be-at-least-32-characters
JWT_EXPIRATION=24h
JWT_REFRESH=7d
JWT_ISSUER=cineverse-api

# ===========================================
# TMDB API CONFIGURATION
# ===========================================
# Get your API key from https://www.themoviedb.org/settings/api
TMDB_API_KEY=
TMDB_BASE_URL=https://api.themoviedb.org/3
TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p/
TMDB_CACHE_TTL=24h
TMDB_RATE_LIMIT=40

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================
# Configure if you want email functionality (registration confirmation, password reset)
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USERNAME=your-email@gmail.com
EMAIL_SMTP_PASSWORD=your-app-password
EMAIL_FROM_EMAIL=noreply@cineverse.com
EMAIL_FROM_NAME=CineVerse
EMAIL_TEMPLATE_DIR=templates/emails

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
BCRYPT_COST=12
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MINUTES=15

# ===========================================
# INTERNATIONALIZATION
# ===========================================
DEFAULT_LANGUAGE=en
SUPPORTED_LANGUAGES=en,pt,es

# ===========================================
# DEVELOPMENT SETTINGS
# ===========================================
LOG_LEVEL=debug
ENABLE_SWAGGER=true

# ===========================================
# EXTERNAL URLS
# ===========================================
BASE_URL=http://localhost:8080
SWAGGER_URL=http://localhost:8080/swagger/doc.json

# ===========================================
# IMPORTANT NOTES
# ===========================================
# 1. Change JWT_SECRET in production!
# 2. Get TMDB_API_KEY from https://www.themoviedb.org/settings/api
# 3. Configure email settings if you want user registration/password reset
# 4. Adjust host ports if they conflict with other services
# 5. For production, use strong passwords and enable SSL

# ===========================================
# JWT CONFIGURATION
# ===========================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-must-be-at-least-32-characters
JWT_EXPIRATION=24h
JWT_REFRESH=7d
JWT_ISSUER=cineverse-api

# ===========================================
# TMDB API CONFIGURATION
# ===========================================
# Get your API key from https://www.themoviedb.org/settings/api
TMDB_API_KEY=
TMDB_BASE_URL=https://api.themoviedb.org/3
TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p/
TMDB_CACHE_TTL=24h
TMDB_RATE_LIMIT=40
# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================
# Configure if you want email functionality (registration confirmation, password reset)
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USERNAME=your-email@gmail.com
EMAIL_SMTP_PASSWORD=your-app-password
EMAIL_FROM_EMAIL=noreply@cineverse.com
EMAIL_FROM_NAME=CineVerse
EMAIL_TEMPLATE_DIR=templates/emails

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
BCRYPT_COST=12
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MINUTES=15

# ===========================================
# INTERNATIONALIZATION
# ===========================================
DEFAULT_LANGUAGE=en
SUPPORTED_LANGUAGES=en,pt,es

# ===========================================
# DEVELOPMENT SETTINGS
# ===========================================
LOG_LEVEL=debug
ENABLE_SWAGGER=true

# ===========================================
# EXTERNAL URLS
# ===========================================
BASE_URL=http://localhost:8080
SWAGGER_URL=http://localhost:8080/swagger/doc.json

# ===========================================
# IMPORTANT NOTES
# ===========================================
# 1. Change JWT_SECRET in production!
# 2. Get TMDB_API_KEY from https://www.themoviedb.org/settings/api
# 3. Configure email settings if you want user registration/password reset
# 4. Adjust host ports if they conflict with other services